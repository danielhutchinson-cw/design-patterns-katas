{
  "name": "strategy-pattern",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "start": "yarn build && node dist/main.js",
    "build": "rimraf dist && tsc",
    "test": "jest",
    "test-session": "jest --watchAll"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.12.2",
    "jest": "^24.5.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.0",
    "typescript": "^3.4.1"
  }
}
